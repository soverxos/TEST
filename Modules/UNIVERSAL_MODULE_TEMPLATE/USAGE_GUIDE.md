# 🚀 Руководство по использованию Универсального Шаблона Модуля

## 📋 Обзор

Универсальный шаблон модуля (`universal_template`) - это полнофункциональный пример модуля для системы SwiftDevBot, демонстрирующий все возможности платформы.

## 🎯 Доступные команды

### В Telegram боте:

1. **`/template`** - Главное меню модуля
   - Просмотр статистики
   - Доступ к основным функциям
   - Навигация по модулю

2. **`/template_admin`** - Административная панель
   - Управление данными
   - Настройки модуля
   - Статистика использования

3. **`/template_fsm`** - Пример FSM диалога
   - Демонстрация многошаговых диалогов
   - Обработка пользовательского ввода
   - Состояния диалога

### Через UI (кнопка "Модули"):
- Нажмите кнопку **"Модули"** в главном меню бота
- Выберите **"Универсальный Шаблон Модуля"**
- Откроется главное меню модуля

## 🔧 Функциональность

### Основные возможности:
- ✅ **CRUD операции** - создание, чтение, обновление, удаление данных
- ✅ **FSM диалоги** - многошаговые взаимодействия с пользователем
- ✅ **Система разрешений** - контроль доступа к функциям
- ✅ **Административная панель** - управление модулем
- ✅ **Статистика и аналитика** - отслеживание использования
- ✅ **Аудит логирование** - запись всех действий
- ✅ **Настройки модуля** - конфигурируемые параметры

### База данных:
- **Таблица `universal_template_data`** - основные данные модуля
- **Таблица `universal_template_user_data`** - пользовательские данные и статистика

## ⚙️ Настройки модуля

Модуль поддерживает следующие настройки:

| Настройка | Тип | Описание | По умолчанию |
|-----------|-----|----------|--------------|
| `enabled` | bool | Включить модуль | `true` |
| `max_items_per_user` | int | Максимум элементов на пользователя | `10` |
| `api_key` | string | API ключ для внешних сервисов | `""` |
| `debug_mode` | bool | Режим отладки | `false` |
| `notification_enabled` | bool | Уведомления пользователям | `true` |
| `max_file_size` | int | Максимальный размер файла (МБ) | `10` |

## 🔐 Система разрешений

Модуль определяет следующие разрешения:

- **`universal_template.access`** - Базовый доступ к модулю
- **`universal_template.view_data`** - Просмотр данных модуля
- **`universal_template.manage_data`** - Управление данными модуля
- **`universal_template.admin`** - Административные функции модуля
- **`universal_template.advanced`** - Продвинутые функции модуля

## 📊 Мониторинг и логи

### Логи модуля:
- Все действия пользователей записываются в аудит лог
- Ошибки и предупреждения логируются в основной лог системы
- Статистика использования сохраняется в базе данных

### CLI команды для мониторинга:
```bash
# Просмотр статуса модуля
python3 sdb.py module list

# Детальная информация о модуле
python3 sdb.py module info universal_template

# Просмотр логов
tail -f project_data/Logs/2025/10-October/10/19_sdb.log
```

## 🛠️ Разработка на основе шаблона

### Создание нового модуля:

1. **Скопируйте шаблон:**
   ```bash
   cp -r modules/UNIVERSAL_MODULE_TEMPLATE modules/my_new_module
   ```

2. **Обновите файлы:**
   - `manifest.yaml` - измените имя, описание, команды
   - `__init__.py` - обновите константы модуля
   - `handlers.py` - адаптируйте обработчики под ваши нужды
   - `models.py` - определите свои модели данных
   - `services.py` - реализуйте бизнес-логику

3. **Активируйте модуль:**
   ```bash
   python3 sdb.py module enable my_new_module
   python3 sdb.py run
   ```

### Структура файлов:
```
modules/my_new_module/
├── manifest.yaml              # Метаданные модуля
├── __init__.py                # Точка входа модуля
├── handlers.py                # Обработчики команд и callback'ов
├── keyboards.py               # Клавиатуры для Telegram
├── models.py                  # Модели базы данных
├── services.py                # Бизнес-логика
├── utils.py                   # Вспомогательные функции
├── permissions.py             # Определения разрешений
├── callback_data_factories.py # Фабрики callback данных
├── module_settings.yaml       # Настройки по умолчанию
├── README.md                  # Документация модуля
├── QUICK_START.md             # Быстрый старт
├── examples/                  # Примеры использования
└── tests/                     # Тесты модуля
```

## 🎓 Примеры использования

### Базовое использование:
```python
# В handlers.py
@router.command("my_command")
async def my_command_handler(message: types.Message):
    # Ваша логика здесь
    pass
```

### Работа с базой данных:
```python
# В services.py
async def create_item(self, user_id: int, data: dict):
    async with self.services.db_manager.get_session() as session:
        item = MyModel(user_id=user_id, **data)
        session.add(item)
        await session.commit()
        return item
```

### FSM диалог:
```python
# В handlers.py
class MyStates(StatesGroup):
    waiting_input = State()
    processing = State()

@router.message(StateFilter(MyStates.waiting_input))
async def process_input(message: types.Message, state: FSMContext):
    # Обработка ввода пользователя
    await state.set_state(MyStates.processing)
```

## 🔍 Отладка

### Включение режима отладки:
1. Откройте настройки модуля: `/template_admin` → "Настройки"
2. Включите "Режим отладки"
3. Перезапустите бота: `python3 sdb.py restart`

### Просмотр логов:
```bash
# Основной лог
tail -f project_data/Logs/2025/10-October/10/19_sdb.log

# Аудит лог
tail -f project_data/Logs/2025/10-October/10/audit.log
```

## 📚 Дополнительные ресурсы

- **QUICK_START.md** - Быстрый старт для разработчиков
- **examples/** - Примеры кода и использования
- **tests/** - Тесты для всех компонентов
- **README.md** - Подробная документация модуля

## 🆘 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте логи системы
2. Убедитесь, что модуль активирован
3. Проверьте настройки модуля
4. Обратитесь к документации SwiftDevBot

---

**Универсальный шаблон модуля** - ваш надежный помощник в создании мощных и функциональных модулей для SwiftDevBot! 🚀
