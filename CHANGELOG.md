# Changelog

## Unreleased
- [ ] Перенести актуальные TODO из архива (если требуется) в рабочую ветку и закрыть нерелевантные.

## Done
- [x] Завершить реализацию `CryptoManager`: получать версию из манифеста, полноценно шифровать приватные ключи (AES-GCM) и убрать комментарии `TODO`.
- [x] Добавить реальные API конечные точки для блокировки пользователей и переключения статуса модулей (web-дэшборд теперь работает с базой и конфигом).
- [x] Перевести ключевые строки админки (`Users`, `Roles`, `SysInfo`) в `get_*_texts`/локализации.
- [x] Добавить тесты FastAPI для новых API `/api/users/{id}/block` и `/api/modules/{name}/toggle`.
- [x] Добавить `make web-test` (через `.venv`) для автоматического прогонки FastAPI-интеграций.

