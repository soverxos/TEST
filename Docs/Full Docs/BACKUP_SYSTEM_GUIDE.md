# 📦 Полное руководство по системе бэкапа SwiftDevBot

> **Версия:** 2.0 (Unified Backup System)  
> **Дата:** 30 августа 2025  
> **Автор:** SwiftDevBot Team  

## 🎯 Содержание

1. [Быстрый старт](#-быстрый-старт)
2. [Команды системы бэкапа](#-команды-системы-бэкапа)
3. [Детальное описание команд](#-детальное-описание-команд)
4. [Практические примеры](#-практические-примеры)
5. [Типы бэкапов](#-типы-бэкапов)
6. [Интеллектуальный анализ проекта](#-интеллектуальный-анализ-проекта)
7. [Управление архивами](#-управление-архивами)
8. [Восстановление данных](#-восстановление-данных)
9. [Устранение неисправностей](#-устранение-неисправностей)
10. [Лучшие практики](#-лучшие-практики)

---

## ⚡ Быстрый старт

### Базовые команды для немедленного использования:

```bash
# 🔍 Проверить конфигурацию проекта
python sdb.py backup check

# 📦 Создать полный бэкап (рекомендуется)
python sdb.py backup create --type=full

# 📋 Посмотреть все бэкапы
python sdb.py backup list

# ℹ️ Информация о конкретном бэкапе
python sdb.py backup info backup_name.tar.gz

# ✅ Проверить целостность бэкапа
python sdb.py backup verify backup_name.tar.gz

# 🔄 Восстановить из бэкапа
python sdb.py backup restore backup_name.tar.gz
```

---

## 🛠 Команды системы бэкапа

Система бэкапа SwiftDevBot предоставляет **6 основных команд**:

| Команда | Назначение | Синтаксис |
|---------|------------|-----------|
| `create` | Создание бэкапа | `python sdb.py backup create [опции]` |
| `list` | Список бэкапов | `python sdb.py backup list [опции]` |
| `restore` | Восстановление | `python sdb.py backup restore <архив>` |
| `verify` | Проверка целостности | `python sdb.py backup verify <архив>` |
| `info` | Детальная информация | `python sdb.py backup info <архив>` |
| `check` | Анализ проекта | `python sdb.py backup check [путь]` |

---

## 📖 Детальное описание команд

### 🔍 `check` - Анализ конфигурации проекта

**Назначение:** Анализирует проект и даёт рекомендации по созданию бэкапа.

**Синтаксис:**
```bash
python sdb.py backup check [путь]
```

**Параметры:**
- `путь` (опционально) - путь для анализа (по умолчанию - корень проекта)

**Пример вывода:**
```
🔍 Анализ конфигурации проекта: /root/SDB-TEST
╭────── 📋 Анализ области бэкапа ───────╮
│ 📍 Анализируемый путь: /root/SDB-TEST │
│ 🏠 Корень проекта: ✅                 │
│ 🌐 Внешний путь: ✅                   │
│ 📁 Включает файлы проекта: ✅         │
╰───────────────────────────────────────╯
╭───── 🗃️ Конфигурация БД ─────╮
│ 🗃️ Тип БД: SQLITE           │
│ 📍 Местоположение: metrics.db│
│ 🔄 Нужен отдельный бэкап: ❌ │
│ 📄 Обнаружены файлы: 2      │
╰─────────────────────────────╯

🎯 Рекомендуемые команды бэкапа:
  # Полный бэкап проекта (рекомендуется)
  python sdb.py backup create --type=full
```

### 📦 `create` - Создание бэкапа

**Назначение:** Создаёт архив с файлами проекта и/или базой данных.

**Синтаксис:**
```bash
python sdb.py backup create --type=<тип> [опции]
```

**Обязательные параметры:**
- `--type` / `-t` - тип бэкапа (`full`, `files`, `db`)

**Опциональные параметры:**
- `--name` / `-n` - кастомное имя бэкапа
- `--exclude` / `-e` - дополнительные исключения
- `--db-url` - URL базы данных (для типа `db`)

**Типы бэкапов:**
1. **`full`** - полный бэкап (файлы + база данных)
2. **`files`** - только файлы проекта
3. **`db`** - только база данных

### 📋 `list` - Список бэкапов

**Назначение:** Показывает все доступные бэкапы с их характеристиками.

**Синтаксис:**
```bash
python sdb.py backup list [опции]
```

**Опциональные параметры:**
- `--type` / `-t` - фильтр по типу (`all`, `files`, `db`, `unified`)
- `--show-hashes` - показать информацию о хешах

**Пример вывода:**
```
📋 Список доступных бэкапов
╭─────────────────────────────────────────────────────╮
│ 📦 unified_full_20250830_105345.tar.gz             │
│ 📅 30.08.2025 10:53    📊 0.4MB    🗜️ 84.4%        │
│ 📁 Файлы: ✅ (156)     🗃️ БД: ✅     🔍 Хеши: ✅   │
╰─────────────────────────────────────────────────────╯
```

### ℹ️ `info` - Детальная информация

**Назначение:** Показывает подробную информацию о конкретном бэкапе.

**Синтаксис:**
```bash
python sdb.py backup info <имя_файла>
```

**Параметры:**
- `имя_файла` - имя архива бэкапа (с расширением или без)

### ✅ `verify` - Проверка целостности

**Назначение:** Проверяет целостность архива и соответствие хешей.

**Синтаксис:**
```bash
python sdb.py backup verify <имя_файла>
```

### 🔄 `restore` - Восстановление

**Назначение:** Восстанавливает файлы из архива с проверкой целостности.

**Синтаксис:**
```bash
python sdb.py backup restore <имя_файла> [опции]
```

**Опциональные параметры:**
- `--target` / `-t` - целевая папка (по умолчанию - `./restored`)
- `--verify-hashes` - включить проверку хешей (по умолчанию `true`)

---

## 💡 Практические примеры

### 🏠 Сценарий 1: Ежедневный бэкап проекта

```bash
# Проверяем конфигурацию проекта
python sdb.py backup check

# Создаём полный бэкап с кастомным именем
python sdb.py backup create --type=full --name="daily_backup"

# Проверяем, что бэкап создался
python sdb.py backup list

# Результат: daily_backup_20250830_120000.tar.gz
```

### 🗃️ Сценарий 2: Только база данных

```bash
# Если у вас есть внешняя БД (PostgreSQL/MySQL)
python sdb.py backup create --type=db --db-url="postgresql://user:pass@localhost/dbname"

# Проверяем бэкап БД
python sdb.py backup verify unified_db_20250830_120500.tar.gz
```

### 📁 Сценарий 3: Только файлы (без БД)

```bash
# Создаём бэкап только файлов (подходит если БД уже забэкаплена отдельно)
python sdb.py backup create --type=files --exclude="logs,temp"

# Смотрим что получилось
python sdb.py backup info unified_files_20250830_121000.tar.gz
```

### 🔄 Сценарий 4: Восстановление после сбоя

```bash
# Смотрим доступные бэкапы
python sdb.py backup list

# Проверяем целостность перед восстановлением
python sdb.py backup verify unified_full_20250829_180000.tar.gz

# Восстанавливаем в отдельную папку
python sdb.py backup restore unified_full_20250829_180000.tar.gz --target="/tmp/recovery"

# Проверяем восстановленные файлы
ls -la /tmp/recovery/
```

### 🔍 Сценарий 5: Анализ внешнего проекта

```bash
# Анализируем проект в другой папке
python sdb.py backup check /path/to/another/project

# Результат покажет рекомендации для внешнего проекта:
# ⚠️ Указанный путь находится ВНЕ проекта SwiftDevBot
# 🎯 Рекомендация: python sdb.py backup create --type=custom /path/to/another/project
```

### 📊 Сценарий 6: Мониторинг бэкапов

```bash
# Смотрим все бэкапы с хешами
python sdb.py backup list --show-hashes

# Проверяем целостность всех бэкапов (скрипт)
for backup in backup/*.tar.gz; do
    echo "Проверяем: $backup"
    python sdb.py backup verify "$backup"
done
```

---

## 🎯 Типы бэкапов

### 🌟 `FULL` (Полный бэкап)
- **Содержит:** файлы проекта + база данных
- **Рекомендуется для:** ежедневных бэкапов, миграций системы
- **Размер:** максимальный (включает всё)
- **Время создания:** среднее

**Пример создания:**
```bash
python sdb.py backup create --type=full
# Результат: unified_full_20250830_HHMMSS.tar.gz
```

### 📁 `FILES` (Только файлы)
- **Содержит:** исходный код, конфигурации, документация
- **Рекомендуется для:** частых снимков кода, версионирования
- **Размер:** средний (без БД)
- **Время создания:** быстрое

**Пример создания:**
```bash
python sdb.py backup create --type=files
# Результат: unified_files_20250830_HHMMSS.tar.gz
```

### 🗃️ `DB` (Только база данных)
- **Содержит:** дамп базы данных
- **Рекомендуется для:** критичных данных, перед обновлениями БД
- **Размер:** зависит от размера БД
- **Время создания:** зависит от БД

**Пример создания:**
```bash
# Для внешней БД (PostgreSQL/MySQL)
python sdb.py backup create --type=db --db-url="postgresql://user:pass@localhost/dbname"

# Для SQLite (автоматически включается в files/full)
python sdb.py backup create --type=full  # SQLite уже включён
```

---

## 🧠 Интеллектуальный анализ проекта

Система автоматически анализирует ваш проект и определяет:

### 📊 Анализ области бэкапа

| Параметр | Описание | Возможные значения |
|----------|----------|-------------------|
| **Корень проекта** | Находимся ли в SwiftDevBot | ✅ Да / ❌ Нет |
| **Внешний путь** | Путь вне основного проекта | ✅ Нормально / ⚠️ Внешний |
| **Включает файлы проекта** | Есть ли важные папки | ✅ Да / ❌ Нет |

### 🗃️ Анализ базы данных

| Тип БД | Детекция | Действие |
|--------|----------|----------|
| **SQLite** | `*.db`, `*.sqlite`, `*.sqlite3` | Включается в файловый бэкап |
| **PostgreSQL** | `postgresql://` в конфигах | Нужен отдельный бэкап БД |
| **MySQL** | `mysql://` в конфигах | Нужен отдельный бэкап БД |
| **Нет БД** | Файлы не найдены | Только файлы |

### ⚠️ Предупреждения и рекомендации

**Примеры предупреждений:**
- `⚠️ Указанный путь находится ВНЕ проекта SwiftDevBot`
- `⚠️ Не включены важные директории: core, cli, modules`
- `⚠️ База данных требует отдельного бэкапа`

**Примеры рекомендаций:**
- `💡 Для бэкапа проекта SwiftDevBot используйте корневую папку`
- `💡 Рассмотрите возможность полного бэкапа: --type=full`
- `💡 SQLite файлы уже включены в файловый бэкап`

---

## 📚 Управление архивами

### 📁 Структура папки бэкапов

```
backup/
├── unified_full_20250830_105345.tar.gz      # Полный бэкап
├── unified_files_20250830_104446.tar.gz     # Только файлы  
├── unified_db_20250830_103000.tar.gz        # Только БД
└── smart_20250830_102532.tar.gz             # Старый формат (deprecated)
```

### 🏷️ Именование файлов

**Формат:** `unified_{тип}_{дата}_{время}.tar.gz`

**Примеры:**
- `unified_full_20250830_143022.tar.gz` - полный бэкап от 30.08.2025 14:30:22
- `unified_files_20250830_143022.tar.gz` - файлы от 30.08.2025 14:30:22
- `unified_db_20250830_143022.tar.gz` - БД от 30.08.2025 14:30:22

### 📊 Метаданные архива

Каждый архив содержит файл `metadata.json`:
```json
{
  "type": "unified_full",
  "timestamp": "2025-08-30T14:30:22",
  "files": {
    "count": 156,
    "size_bytes": 2834567,
    "include_hashes": true
  },
  "database": {
    "type": "sqlite", 
    "success": true,
    "detected_files": ["project_data/monitor/metrics.db"]
  },
  "compression": {
    "enabled": true,
    "ratio": 0.844
  }
}
```

---

## 🔄 Восстановление данных

### ⚡ Быстрое восстановление

```bash
# Восстановить в папку ./restored
python sdb.py backup restore unified_full_20250830_105345.tar.gz

# Восстановить в кастомную папку
python sdb.py backup restore unified_full_20250830_105345.tar.gz --target="/tmp/recovery"
```

### 🔍 Восстановление с проверкой

```bash
# Включить проверку хешей (по умолчанию)
python sdb.py backup restore backup.tar.gz --verify-hashes=true

# Отключить проверку хешей (быстрее, но менее безопасно)
python sdb.py backup restore backup.tar.gz --verify-hashes=false
```

### 📂 Структура восстановленных файлов

```
restored/
├── files/                    # Файлы проекта
│   ├── sdb.py
│   ├── core/
│   ├── cli/
│   └── ...
├── database/                 # Файлы БД (если есть)
│   └── dump.sql
└── metadata.json            # Метаданные бэкапа
```

### ⚠️ Безопасность восстановления

1. **Проверка хешей** - автоматически проверяет целостность файлов
2. **Изолированное восстановление** - не перезаписывает текущие файлы
3. **Логирование** - полный лог процесса восстановления
4. **Откат при ошибках** - остановка при обнаружении поврежденных данных

---

## 🔧 Устранение неисправностей

### ❌ Типичные ошибки и их решения

#### 1. **"Бэкап не найден"**
```bash
# Ошибка:
Неожиданная ошибка: [Errno 2] No such file or directory: 'backup.tar.gz'

# Решение:
# Укажите полный путь или проверьте название файла
python sdb.py backup list                    # Посмотрите доступные бэкапы
python sdb.py backup info backup/полное_имя.tar.gz  # Используйте полный путь
```

#### 2. **"Нет утилит базы данных"**
```bash
# Ошибка:
⚠️ Бэкап БД пропущен (нет БД или утилит)

# Решение для PostgreSQL:
sudo apt-get install postgresql-client

# Решение для MySQL:
sudo apt-get install mysql-client
```

#### 3. **"Повреждённый архив"**
```bash
# Ошибка:
❌ Архив повреждён или не читается

# Диагностика:
python sdb.py backup verify имя_архива.tar.gz

# Если архив повреждён:
# - Используйте другой бэкап
# - Проверьте свободное место на диске
# - Проверьте права доступа к файлам
```

#### 4. **"Ошибка проверки хешей"**
```bash
# Ошибка:
❌ Хеш файла не совпадает: ожидался abc123, получен def456

# Решение:
# - Файл был изменён после создания бэкапа
# - Возможное повреждение данных
# - Используйте другой бэкап или отключите проверку хешей:
python sdb.py backup restore архив.tar.gz --verify-hashes=false
```

### 🔍 Диагностические команды

```bash
# Проверить состояние системы бэкапа
python sdb.py backup check

# Подробная информация об архиве
python sdb.py backup info архив.tar.gz

# Проверить целостность без восстановления
python sdb.py backup verify архив.tar.gz

# Посмотреть логи в процессе создания
python sdb.py backup create --type=full --name="diagnostic"
```

### 📝 Логирование

Все операции логируются в:
- **Консоль** - основная информация с временными метками
- **Система логирования бота** - подробные логи всех операций

Пример лога:
```
⏰ Начало: 11:00:18.347
🔍 Анализ конфигурации проекта: /root/SDB-TEST
✅ Найдены SQLite файлы: project_data/monitor/metrics.db
📦 Копирование файлов...
📊 Размер архива: 0.4MB
⏰ Окончание: 11:00:20.525
⚡ Время выполнения: 2.178 секунд
```

---

## 🏆 Лучшие практики

### 📅 Регулярность бэкапов

**Рекомендуемая схема:**
- **Ежедневно:** `python sdb.py backup create --type=full` (полный бэкап)
- **Каждые 4 часа:** `python sdb.py backup create --type=files` (только код)
- **Перед обновлениями:** `python sdb.py backup create --type=full --name="pre_update"`

### 🗂️ Управление архивами

```bash
# Скрипт автоматической очистки старых бэкапов (оставляем последние 7 дней)
find backup/ -name "*.tar.gz" -mtime +7 -delete

# Скрипт проверки всех бэкапов
for backup in backup/*.tar.gz; do
    echo "Проверяем: $backup"
    python sdb.py backup verify "$backup"
done
```

### 💾 Хранение бэкапов

**Локальное хранение:**
- Папка `backup/` в корне проекта
- Права доступа: только владелец (`chmod 700 backup/`)

**Внешнее хранение:**
```bash
# Копирование на внешний сервер
rsync -az backup/ user@server:/backups/swiftdevbot/

# Загрузка в облако
rclone sync backup/ dropbox:SwiftDevBot/backups/
```

### 🔐 Безопасность

1. **Шифрование архивов:**
```bash
# Создать бэкап и зашифровать
python sdb.py backup create --type=full
gpg --cipher-algo AES256 --compress-algo 1 --s2k-mode 3 \
    --s2k-digest-algo SHA512 --s2k-count 65536 --symmetric \
    backup/unified_full_*.tar.gz
```

2. **Проверка целостности:**
```bash
# Регулярная проверка всех бэкапов
python sdb.py backup list --show-hashes
```

### ⚡ Оптимизация производительности

**Ускорение бэкапов:**
- Используйте SSD для папки `backup/`
- Исключайте ненужные файлы: `--exclude="logs,temp,.git"`
- Для больших проектов используйте `--type=files` чаще чем `--type=full`

**Экономия места:**
```bash
# Бэкап без сжатия (быстрее создание, больше размер)
# Текущая система всегда использует gzip сжатие для оптимального размера
```

### 🚀 Автоматизация

**Crontab для автоматических бэкапов:**
```bash
# Редактируем crontab
crontab -e

# Добавляем задания:
# Полный бэкап каждый день в 2:00
0 2 * * * cd /root/SDB-TEST && python sdb.py backup create --type=full

# Файловый бэкап каждые 6 часов
0 */6 * * * cd /root/SDB-TEST && python sdb.py backup create --type=files

# Проверка целостности каждое воскресенье в 3:00  
0 3 * * 0 cd /root/SDB-TEST && for f in backup/*.tar.gz; do python sdb.py backup verify "$f"; done
```

**Systemd timer (альтернатива cron):**
```bash
# /etc/systemd/system/swiftdevbot-backup.timer
[Unit]
Description=SwiftDevBot Daily Backup
Requires=swiftdevbot-backup.service

[Timer]
OnCalendar=daily
Persistent=true

[Install]
WantedBy=timers.target
```

---

## 📋 Заключение

Система бэкапа SwiftDevBot предоставляет:

✅ **Полную автоматизацию** - умная детекция типов БД и проектов  
✅ **Надёжность** - проверка целостности через SHA256 хеши  
✅ **Гибкость** - различные типы бэкапов для разных сценариев  
✅ **Простоту** - интуитивные команды с подробной документацией  
✅ **Безопасность** - изолированное восстановление и проверка данных  

### 🆘 Нужна помощь?

1. **Проверьте конфигурацию:** `python sdb.py backup check`
2. **Смотрите логи** в выводе команд  
3. **Используйте verify** для диагностики архивов
4. **Обращайтесь к разделу "Устранение неисправностей"**

---

> **💡 Совет:** Начните с команды `python sdb.py backup check` - она проанализирует ваш проект и даст персональные рекомендации!

**Happy Backing Up! 🚀**
