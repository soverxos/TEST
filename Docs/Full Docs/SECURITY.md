# SECURITY.md
# 🛡️ Система безопасности SDB

## Обзор

SDB включает комплексную систему безопасности для защиты от зловредных модулей и обеспечения безопасной работы бота. Система состоит из нескольких взаимосвязанных компонентов.

## 🔐 Компоненты системы безопасности

### 1. Система цифровых подписей (ModuleSignatureManager)
- **Назначение**: Проверка целостности и подлинности модулей
- **Функции**:
  - Подписание модулей цифровыми подписями
  - Проверка подписей при загрузке
  - Управление доверенными подписантами
  - Верификация хэшей файлов

### 2. Sandbox-окружение (ModuleSandboxManager)
- **Назначение**: Изоляция модулей с ограниченными правами
- **Уровни безопасности**:
  - `PARANOID`: Максимальная изоляция
  - `STRICT`: Строгие ограничения
  - `MODERATE`: Умеренные ограничения
  - `PERMISSIVE`: Минимальные ограничения (только для разработки)
- **Ограничения**:
  - Доступ к файловой системе
  - Сетевые запросы
  - Системные команды
  - Использование памяти и времени выполнения

### 3. Аудит-логи (SecurityAuditLogger)
- **Назначение**: Запись всех действий модулей для мониторинга
- **Типы событий**:
  - Загрузка/выгрузка модулей
  - Выполнение команд
  - Доступ к базе данных
  - Файловые операции
  - Сетевые запросы
  - Нарушения безопасности
- **Формат**: JSONL файлы с временными метками

### 4. Система репутации (ModuleReputationSystem)
- **Назначение**: Оценка надежности модулей и разработчиков
- **Факторы репутации**:
  - Валидность подписи (25%)
  - Качество кода (20%)
  - Результат сканирования безопасности (20%)
  - Отзывы пользователей (15%)
  - Время активности (10%)
  - Нарушения (-20%)
  - Частота обновлений (10%)
- **Уровни репутации**:
  - `VERIFIED` (81-100): Проверенные модули
  - `TRUSTED` (61-80): Доверенные модули
  - `NEUTRAL` (41-60): Нейтральные модули
  - `SUSPICIOUS` (21-40): Подозрительные модули
  - `UNTRUSTED` (0-20): Недоверенные модули

### 5. Сканер кода (ModuleCodeScanner)
- **Назначение**: Автоматический анализ кода на предмет угроз
- **Типы угроз**:
  - Подозрительные импорты
  - Системные команды
  - Доступ к файлам
  - Сетевые запросы
  - Инъекция кода
  - Backdoor функции
  - Криптомайнинг
- **Анализ**: Статический анализ AST + регулярные выражения

### 6. Уровни безопасности (SecurityLevelManager)
- **Назначение**: Настройка политик безопасности
- **Политики**:
  - `ALLOW_SIGNED_ONLY`: Только подписанные модули
  - `REQUIRE_REPUTATION`: Требовать минимальную репутацию
  - `SANDBOX_ALL_MODULES`: Все модули в sandbox
  - `AUDIT_ALL_ACTIONS`: Аудит всех действий
  - `BLOCK_SUSPICIOUS`: Блокировать подозрительные модули
  - `REQUIRE_APPROVAL`: Требовать одобрение администратора

### 7. Детекция аномалий (AnomalyDetector)
- **Назначение**: Мониторинг и выявление подозрительного поведения
- **Типы аномалий**:
  - Слишком частые команды
  - Необычные паттерны использования
  - Подозрительное время активности
  - Злоупотребление ресурсами
  - Подозрение на утечку данных
- **Автоматические действия**:
  - Блокировка модулей при критических аномалиях
  - Уведомления администраторов
  - Запись в аудит-логи

## 🚀 Использование

### CLI команды

```bash
# Показать статус системы безопасности
./sdb security status

# Установить уровень безопасности
./sdb security set-level paranoid

# Сканировать модуль
./sdb security scan-module /path/to/module

# Показать аномалии
./sdb security list-anomalies --hours 24

# Показать репутацию модулей
./sdb security reputation

# Показать аудит-логи
./sdb security audit-logs --hours 24
```

### Программный доступ

```python
from core.services_provider import BotServicesProvider

# Получение сервисов безопасности
services = BotServicesProvider(settings)

# Проверка подписи модуля
signature_result = services.signature_manager.verify_signature(module_path)

# Сканирование модуля
scan_result = services.code_scanner.scan_module(module_path)

# Проверка разрешений
has_permission = services.sandbox_manager.check_permission(
    module_name, "database"
)

# Запись события аудита
services.audit_logger.log_module_load(
    module_name, module_path, signature_valid=True
)

# Анализ активности на аномалии
anomalies = services.anomaly_detector.analyze_activity(
    module_name, "command", user_id=123
)
```

## ⚙️ Конфигурация

### Уровни безопасности

1. **PARANOID** (Параноидальный):
   - Только подписанные модули от доверенных разработчиков
   - Максимальная изоляция
   - Требуется одобрение администратора
   - Максимальный риск: 10/100

2. **STRICT** (Строгий):
   - Только подписанные модули
   - Строгие ограничения sandbox
   - Минимальная репутация: 60/100
   - Максимальный риск: 30/100

3. **MODERATE** (Умеренный):
   - Требуется репутация 40/100
   - Умеренные ограничения sandbox
   - Максимальный риск: 50/100

4. **PERMISSIVE** (Разрешающий):
   - Минимальные ограничения
   - Только для разработки
   - Максимальный риск: 100/100

### Пороги детекции аномалий

```python
thresholds = {
    "max_commands_per_minute": 30,
    "max_commands_per_hour": 500,
    "max_file_access_per_minute": 20,
    "max_network_requests_per_minute": 10,
    "max_database_queries_per_minute": 100,
    "suspicious_hours": [0, 1, 2, 3, 4, 5],  # Ночные часы
    "unusual_pattern_threshold": 0.7
}
```

## 📁 Структура файлов

```
project_data/Security/
├── signatures/           # Цифровые подписи модулей
├── trusted_keys/         # Доверенные ключи
├── sandbox/             # Sandbox окружения модулей
├── audit_logs/          # Аудит-логи
├── reputation/          # Данные репутации
└── anomaly_detection/   # Данные детекции аномалий
```

## 🔧 Интеграция с модулями

### Проверка безопасности при загрузке

```python
# В ModuleLoader
def load_module(self, module_path):
    # 1. Проверка подписи
    signature_result = self.services.signature_manager.verify_signature(module_path)
    
    # 2. Сканирование кода
    scan_result = self.services.code_scanner.scan_module(module_path)
    
    # 3. Проверка репутации
    reputation = self.services.reputation_system.get_module_reputation(module_name)
    
    # 4. Проверка политик безопасности
    is_allowed, reason = self.services.security_level_manager.is_module_allowed(
        module_name, signature_result.is_valid, 
        reputation.total_score if reputation else 0,
        scan_result.risk_score
    )
    
    if not is_allowed:
        raise SecurityError(f"Модуль заблокирован: {reason}")
    
    # 5. Создание sandbox
    self.services.sandbox_manager.create_sandbox(module_name, SecurityLevel.MODERATE)
    
    # 6. Запись в аудит
    self.services.audit_logger.log_module_load(module_name, module_path, signature_result.is_valid)
```

### Мониторинг активности

```python
# В обработчиках команд модулей
async def handle_command(self, message, services):
    # Анализ на аномалии
    anomalies = services.anomaly_detector.analyze_activity(
        self.module_name, "command", message.from_user.id
    )
    
    # Проверка на блокировку
    should_block, reason = services.anomaly_detector.should_block_module(
        self.module_name, message.from_user.id
    )
    
    if should_block:
        await message.answer(f"Модуль заблокирован: {reason}")
        return
    
    # Проверка разрешений sandbox
    if not services.sandbox_manager.check_permission(self.module_name, "database"):
        await message.answer("Нет разрешения на доступ к базе данных")
        return
    
    # Выполнение команды
    # ...
    
    # Запись в аудит
    services.audit_logger.log_command_execution(
        self.module_name, command, message.from_user.id, success=True
    )
```

## 🚨 Реагирование на угрозы

### Автоматические действия

1. **Критические угрозы**: Немедленная блокировка модуля
2. **Высокие угрозы**: Блокировка после 3 нарушений
3. **Средние угрозы**: Уведомление администратора
4. **Низкие угрозы**: Запись в аудит-логи

### Уведомления

- Все критические события записываются в аудит-логи
- Администраторы получают уведомления о подозрительной активности
- Автоматическая блокировка опасных модулей

## 📊 Мониторинг и отчеты

### Статистика безопасности

- Общее количество аномалий
- Распределение по типам угроз
- Заблокированные модули
- Активные модули
- Статистика репутации

### Дашборд безопасности

```bash
# Полная сводка по безопасности
./sdb security status

# Детальная статистика
./sdb security list-anomalies --hours 168  # За неделю
./sdb security audit-logs --hours 24      # За день
```

## 🔄 Обновления и обслуживание

### Очистка данных

- Аудит-логи: автоматическая очистка через 30 дней
- Данные аномалий: очистка через 30 дней
- Sandbox окружения: очистка при выгрузке модулей

### Обновление политик

- Политики безопасности можно изменять через CLI
- Изменения применяются немедленно
- Все изменения записываются в аудит-логи

## 🛠️ Разработка безопасных модулей

### Рекомендации

1. **Подписывайте модули**: Используйте цифровые подписи
2. **Минимизируйте права**: Запрашивайте только необходимые разрешения
3. **Качественный код**: Следуйте стандартам кодирования
4. **Документируйте**: Описывайте функциональность модуля
5. **Тестируйте**: Проверяйте модуль сканером безопасности

### Пример безопасного модуля

```python
# Безопасный модуль
class SafeModule:
    def __init__(self, services):
        self.services = services
        self.module_name = "safe_module"
    
    async def handle_command(self, message):
        # Проверяем разрешения
        if not self.services.sandbox_manager.check_permission(self.module_name, "user_data"):
            await message.answer("Нет разрешения на доступ к данным пользователей")
            return
        
        # Логируем активность
        self.services.audit_logger.log_command_execution(
            self.module_name, "safe_command", message.from_user.id
        )
        
        # Безопасное выполнение
        try:
            result = await self.safe_operation()
            await message.answer(f"Результат: {result}")
        except Exception as e:
            self.services.audit_logger.log_command_execution(
                self.module_name, "safe_command", message.from_user.id, 
                success=False, error_message=str(e)
            )
```

## 📞 Поддержка

При возникновении проблем с системой безопасности:

1. Проверьте логи аудита: `./sdb security audit-logs`
2. Проанализируйте аномалии: `./sdb security list-anomalies`
3. Проверьте статус системы: `./sdb security status`
4. Обратитесь к администратору системы

---

**⚠️ Важно**: Система безопасности SDB обеспечивает многоуровневую защиту, но не может гарантировать 100% безопасность. Регулярно обновляйте политики безопасности и мониторьте активность модулей.
