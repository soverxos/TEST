# 📚 ДОКУМЕНТАЦИЯ ПО БЕЗОПАСНОСТИ SDB

Полная документация по системе безопасности и цифровых подписей SDB.

---

## 📋 **СОДЕРЖАНИЕ ДОКУМЕНТАЦИИ**

### **📖 Основные документы**

1. **[Команды системы безопасности](SECURITY_COMMANDS.md)** - Полное руководство по всем командам
2. **[Быстрая справочная карточка](SECURITY_COMMANDS_QUICK_REFERENCE.md)** - Краткий справочник команд
3. **[Руководство по цифровым подписям](../../DIGITAL_SIGNATURES.md)** - Подробное объяснение работы подписей
4. **[Быстрый старт](../../QUICK_START_GUIDE.md)** - Руководство для начинающих
5. **[Общая документация по безопасности](../../SECURITY.md)** - Обзор системы безопасности

---

## 🚀 **БЫСТРЫЙ СТАРТ**

### **Для разработчиков модулей:**
```bash
# 1. Создать ключи
python3 cli/security.py keys-generate my_key

# 2. Подписать модуль
python3 cli/security.py sign-module modules/my_module.py my_key

# 3. Экспортировать публичный ключ
python3 cli/security.py keys-export my_key --output-file my_key.pem
```

### **Для администраторов системы:**
```bash
# 1. Импортировать доверенный ключ
python3 cli/security.py keys-import trusted_dev --public-key-file my_key.pem

# 2. Проверить статус системы
python3 cli/security.py modules-status

# 3. Просмотреть логи аудита
python3 cli/security.py audit-logs --hours 24
```

---

## 🔐 **ОСНОВНЫЕ ПРИНЦИПЫ**

### **Цифровые подписи**
- **Приватный ключ** - секретный, остается только у разработчика
- **Публичный ключ** - можно показывать всем, используется для проверки
- **Подпись модуля** - создается приватным ключом, проверяется публичным

### **Процесс работы**
1. **Разработчик** создает ключи и подписывает модули
2. **Администратор** импортирует публичный ключ в доверенные
3. **Система** автоматически проверяет подписи при загрузке модулей

### **Защита от подделки**
- ❌ Изменение модуля → хэш не совпадает → блокировка
- ❌ Подделка подписи → невозможно без приватного ключа → блокировка
- ❌ Использование чужого ключа → нет приватного ключа → блокировка

---

## 📊 **КАТЕГОРИИ КОМАНД**

### **🔑 Управление ключами**
- `keys-generate` - Создание ключей
- `keys-list` - Просмотр ключей
- `keys-export` - Экспорт публичного ключа
- `keys-import` - Импорт доверенного ключа
- `keys-delete` - Удаление ключа

### **✍️ Подписание модулей**
- `sign-module` - Подписание модуля
- `modules-scan` - Проверка подписи

### **🛡️ Управление безопасностью**
- `modules-status` - Статус системы
- `trusted-list` - Список доверенных ключей
- `trusted-add` - Добавление в доверенные
- `trusted-remove` - Удаление из доверенных

### **🔍 Мониторинг и аудит**
- `audit-logs` - Логи аудита
- `modules-anomalies` - Обнаружение аномалий
- `modules-reputation` - Репутация модулей
- `code-scan` - Сканирование кода

### **⚙️ Настройки**
- `security-level` - Уровень безопасности
- `policies-set` - Политики безопасности
- `report-generate` - Генерация отчетов

---

## 🎯 **ПРАКТИЧЕСКИЕ ПРИМЕРЫ**

### **Полный цикл разработки**
```bash
# Разработчик
python3 cli/security.py keys-generate weather_dev
python3 cli/security.py sign-module modules/weather.py weather_dev
python3 cli/security.py keys-export weather_dev --output-file weather_key.pem

# Администратор
python3 cli/security.py keys-import weather_trusted --public-key-file weather_key.pem
cp weather.py modules/
cp weather.sig modules/

# Система автоматически проверяет подпись
```

### **Корпоративное использование**
```bash
# Настройка строгой безопасности
python3 cli/security.py security-level-set STRICT
python3 cli/security.py policies-set require_signatures true
python3 cli/security.py policies-set min_reputation_score 70

# Мониторинг
python3 cli/security.py modules-status --detailed
python3 cli/security.py modules-anomalies --hours 24
```

---

## ⚠️ **ВАЖНЫЕ ЗАМЕЧАНИЯ**

1. **Безопасность ключей**
   - Приватные ключи никогда не передаются
   - Регулярно обновляйте ключи (каждые 2-3 года)
   - Делайте резервные копии ключей

2. **Мониторинг системы**
   - Регулярно проверяйте статус системы
   - Просматривайте логи аудита
   - Отслеживайте аномалии

3. **Политики безопасности**
   - Устанавливайте соответствующие уровни безопасности
   - Настраивайте политики под ваши потребности
   - Регулярно обновляйте доверенные ключи

---

## 📞 **ПОДДЕРЖКА И ДИАГНОСТИКА**

### **Проверка системы**
```bash
python3 cli/security.py system-check
python3 cli/security.py config-check
python3 cli/security.py integrity-check
```

### **Получение справки**
```bash
python3 cli/security.py --help
python3 cli/security.py <command> --help
```

### **Просмотр логов**
```bash
python3 cli/security.py audit-logs --hours 24
python3 cli/security.py modules-anomalies --hours 168
```

---

## 🔗 **СВЯЗАННЫЕ ДОКУМЕНТЫ**

- **[CLI Documentation](../CLI/)** - Документация по CLI командам
- **[Module Development](../../modules/Create_modules.md)** - Создание модулей
- **[Configuration](../../env.example)** - Конфигурация системы
- **[Requirements](../../requirements.txt)** - Зависимости проекта

---

## 📈 **ВЕРСИОНИРОВАНИЕ**

- **Текущая версия:** SDB Security v0.1.0
- **Последнее обновление:** 2024-12-08
- **Совместимость:** Python 3.8+, aiogram 3.x

---

*Документация по безопасности SDB - Полное руководство по защите модулей*
