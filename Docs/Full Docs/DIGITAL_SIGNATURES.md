# DIGITAL_SIGNATURES.md
# 🔐 Цифровые подписи модулей SDB

## Обзор

SDB использует настоящую криптографию для создания и проверки цифровых подписей модулей. Это обеспечивает **математически невозможную** подделку подписей злоумышленниками.

## 🛡️ Как работает защита

### 1. **Асимметричная криптография RSA**
- **Приватный ключ** - только у разработчика, используется для подписания
- **Публичный ключ** - распространяется всем, используется для проверки
- **Математическая невозможность** подделки без приватного ключа

### 2. **Алгоритм подписания**
```
1. Вычисляется SHA-256 хэш модуля
2. Создается структура данных с метаданными
3. Данные подписываются приватным ключом RSA
4. Подпись сохраняется в файле .sig
```

### 3. **Алгоритм проверки**
```
1. Вычисляется SHA-256 хэш модуля
2. Загружается подпись из файла .sig
3. Проверяется подпись публичным ключом RSA
4. Проверяется принадлежность ключа доверенному подписанту
```

## 🔑 Управление ключами

### Генерация новой пары ключей

```bash
# Генерировать ключ размером 2048 бит
python3 cli/security.py keys-generate my_developer_key --key-size 2048

# Генерировать ключ размером 4096 бит (максимальная безопасность)
python3 cli/security.py keys-generate my_developer_key --key-size 4096
```

### Просмотр доступных ключей

```bash
# Показать все ключи
python3 cli/security.py keys-list
```

### Экспорт публичного ключа

```bash
# Показать публичный ключ в консоли
python3 cli/security.py keys-export my_developer_key

# Сохранить публичный ключ в файл
python3 cli/security.py keys-export my_developer_key --output-file my_key.pub
```

## 📝 Подписание модулей

### Подписание модуля

```bash
# Подписать модуль своим ключом
python3 cli/security.py sign-module /path/to/module my_developer_key
```

### Автоматическая проверка при загрузке

```python
# В ModuleLoader
def load_module(self, module_path):
    # Проверяем подпись
    signature_result = self.services.signature_manager.verify_signature(module_path)
    
    if not signature_result.is_valid:
        raise SecurityError(f"Недействительная подпись: {signature_result.error_message}")
    
    if not signature_result.is_trusted:
        raise SecurityError(f"Ключ {signature_result.key_id} не является доверенным")
    
    # Модуль безопасен для загрузки
    return self._load_module_safely(module_path)
```

## 🔒 Безопасность ключей

### Защита приватных ключей

1. **Шифрование**: Приватные ключи шифруются мастер-паролем
2. **Права доступа**: Файлы ключей доступны только владельцу (0o600)
3. **Изоляция**: Ключи хранятся в защищенной директории
4. **Мастер-пароль**: Генерируется автоматически, хранится в `.master_password`

### Распространение публичных ключей

1. **Безопасный канал**: Публичные ключи передаются по защищенным каналам
2. **Верификация**: Публичные ключи проверяются через доверенные источники
3. **Отпечатки**: Можно использовать отпечатки ключей для дополнительной проверки

## 🏢 Корпоративная безопасность

### Централизованное управление ключами

```bash
# Администратор генерирует корпоративный ключ
python3 cli/security.py keys-generate company_signing_key --key-size 4096

# Экспортирует публичный ключ для распространения
python3 cli/security.py keys-export company_signing_key --output-file company.pub

# Разработчики импортируют доверенный ключ
python3 cli/security.py keys-import company_signing_key --public-key-file company.pub
```

### Политики подписания

1. **Обязательное подписание**: Все модули должны быть подписаны
2. **Доверенные ключи**: Только корпоративные ключи считаются доверенными
3. **Истечение ключей**: Ключи имеют срок действия
4. **Отзыв ключей**: Компрометированные ключи могут быть отозваны

## 🔍 Проверка подписей

### Программная проверка

```python
from core.services_provider import BotServicesProvider

services = BotServicesProvider(settings)

# Проверяем подпись модуля
result = services.signature_manager.verify_signature(module_path)

if result.is_valid and result.is_trusted:
    print(f"✅ Модуль подписан доверенным ключом {result.key_id}")
else:
    print(f"❌ Модуль не прошел проверку: {result.error_message}")
```

### CLI проверка

```bash
# Сканировать модуль (включает проверку подписи)
python3 cli/security.py modules-scan /path/to/module
```

## 🚨 Безопасность против атак

### Защита от подделки

1. **Математическая невозможность**: Без приватного ключа невозможно создать валидную подпись
2. **Криптографическая стойкость**: RSA-2048 обеспечивает защиту на 100+ лет
3. **Хэш-функции**: SHA-256 предотвращает коллизии

### Защита от компрометации

1. **Отзыв ключей**: Компрометированные ключи удаляются из доверенных
2. **Обновление ключей**: Регулярная ротация ключей
3. **Мониторинг**: Аудит всех операций с ключами

### Защита от атак на инфраструктуру

1. **Изоляция ключей**: Ключи хранятся отдельно от кода
2. **Шифрование**: Приватные ключи зашифрованы
3. **Контроль доступа**: Строгие права доступа к файлам

## 📋 Практические рекомендации

### Для разработчиков

1. **Генерируйте ключи**: Создайте свою пару ключей
2. **Подписывайте модули**: Всегда подписывайте свои модули
3. **Защищайте ключи**: Никогда не передавайте приватный ключ
4. **Распространяйте публичный ключ**: Предоставьте публичный ключ администраторам

### Для администраторов

1. **Управляйте доверенными ключами**: Добавляйте только проверенные ключи
2. **Мониторьте подписи**: Отслеживайте все операции подписания
3. **Обновляйте политики**: Регулярно пересматривайте политики безопасности
4. **Резервное копирование**: Создавайте резервные копии ключей

### Для организаций

1. **Корпоративные ключи**: Используйте централизованные ключи
2. **Политики подписания**: Установите строгие правила
3. **Обучение**: Обучите команду работе с ключами
4. **Аудит**: Регулярно проверяйте использование ключей

## 🔧 Технические детали

### Алгоритмы

- **Асимметричная криптография**: RSA-2048/4096
- **Хэш-функция**: SHA-256
- **Шифрование ключей**: PBKDF2-HMAC-SHA256
- **Формат ключей**: PEM (Privacy-Enhanced Mail)

### Структура подписи

```json
{
  "module_name": "example_module",
  "version": "1.0.0",
  "file_hash": "sha256:abc123...",
  "signature": "hex_encoded_signature",
  "signer_key_id": "developer_key_2024",
  "timestamp": 1704067200.0,
  "algorithm": "RSA-SHA256"
}
```

### Файловая структура

```
project_data/Security/
├── crypto_keys/
│   ├── .master_password          # Мастер-пароль (зашифрован)
│   ├── developer_key.private     # Приватный ключ (зашифрован)
│   ├── developer_key.public      # Публичный ключ
│   └── developer_key.meta        # Метаданные ключа
├── signatures/
│   ├── module1.sig               # Подпись модуля 1
│   └── module2.sig               # Подпись модуля 2
└── trusted_keys/
    ├── trusted_signers.json      # Список доверенных подписантов
    └── company_key.public        # Доверенный публичный ключ
```

## ⚠️ Важные предупреждения

### Безопасность ключей

- **НИКОГДА** не передавайте приватный ключ
- **НИКОГДА** не храните ключи в публичных репозиториях
- **ВСЕГДА** используйте сильные пароли
- **РЕГУЛЯРНО** обновляйте ключи

### Проверка подписей

- **ВСЕГДА** проверяйте подписи перед загрузкой модулей
- **НИКОГДА** не доверяйте неподписанным модулям
- **ВСЕГДА** проверяйте принадлежность ключей доверенным подписантам
- **РЕГУЛЯРНО** обновляйте список доверенных ключей

---

**🔐 Цифровые подписи SDB обеспечивают математически невозможную подделку и максимальную безопасность модулей!**
