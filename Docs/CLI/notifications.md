# 🔔 Управление уведомлениями (notifications)

## Обзор

Модуль `notifications` предоставляет базовое управление уведомлениями о запуске и остановке SwiftDevBot. В текущей версии поддерживается только настройка отправки уведомлений администраторам при старте и остановке системы.

## Основные команды

### `python3 sdb.py config notifications --enable/--disable`

Управляет отправкой уведомлений администраторам о запуске и остановке бота.

**Опции:**
- `--enable` - включить уведомления (по умолчанию)
- `--disable` - отключить уведомления

**Примеры:**

```bash
# Отключить уведомления о запуске/остановке
python3 sdb.py config notifications --disable

# Включить уведомления обратно
python3 sdb.py config notifications --enable
```

**Результат при отключении:**
```
🔕 Управление уведомлениями: отключены
Попытка установить core.enable_startup_shutdown_notifications = 'false'...
✅ Настройка успешно обновлена
```

**Результат при включении:**
```
🔕 Управление уведомлениями: включены
Попытка установить core.enable_startup_shutdown_notifications = 'true'...
✅ Настройка успешно обновлена
```

## Что управляется

### ✅ Включено в управление:
- **Уведомления о запуске бота** - сообщение админам при старте системы
- **Уведомления об остановке бота** - сообщение админам при остановке системы

### ❌ НЕ управляется:
- Уведомления от модулей бота
- Системные логи и сообщения
- Пользовательские уведомления
- Email уведомления
- Telegram уведомления (кроме startup/shutdown)

## Примеры уведомлений

### При запуске (если включено):
```
🚀 SDB Core v0.1.0

⚡ Система активирована
🔧 Модули загружены
🌐 API подключен

@YourBotName готов к работе! 🎯
```

### При остановке (если включено):
```
🔄 SDB Core v0.1.0

⏳ Система деактивируется
🛡️ Завершение процессов
💾 Сохранение данных

@YourBotName переходит в режим ожидания...
```

## Настройка

### Через CLI:
```bash
# Отключить для разработки
python3 sdb.py config notifications --disable

# Включить для продакшена
python3 sdb.py config notifications --enable
```

### Через конфигурационный файл:
```yaml
# /root/SDB-TEST/project_data/Config/core_settings.yaml
core:
  enable_startup_shutdown_notifications: false  # или true
```

### Через переменные окружения:
```bash
# Отключить
export SDB_CORE_ENABLE_STARTUP_SHUTDOWN_NOTIFICATIONS=false

# Включить
export SDB_CORE_ENABLE_STARTUP_SHUTDOWN_NOTIFICATIONS=true
```

## Приоритет настроек

1. **Переменные окружения** (высший приоритет)
2. **YAML конфигурация** 
3. **Значения по умолчанию** (низший приоритет)

## Рекомендации

### Для разработки:
```bash
# Отключить уведомления при частых перезапусках
python3 sdb.py config notifications --disable
```

### Для продакшена:
```bash
# Включить уведомления для мониторинга
python3 sdb.py config notifications --enable
```

## Устранение проблем

### Проблема: "Уведомления не отправляются"
```bash
# Проверить настройку
python3 sdb.py config get core.enable_startup_shutdown_notifications

# Включить если отключены
python3 sdb.py config notifications --enable
```

### Проблема: "Уведомления приходят слишком часто"
```bash
# Отключить для разработки
python3 sdb.py config notifications --disable
```

### Проблема: "Неверные настройки"
```bash
# Сбросить к значениям по умолчанию
python3 sdb.py config set core.enable_startup_shutdown_notifications true
```

## Планы развития

В будущих версиях планируется добавить:

### 🔮 Расширенная система уведомлений:
- **Email уведомления** - отправка на email адреса
- **Telegram каналы** - отправка в каналы и группы
- **Webhook уведомления** - интеграция с внешними системами
- **Шаблоны уведомлений** - кастомизация сообщений
- **Планирование** - регулярные уведомления по расписанию
- **Приоритеты** - разные уровни важности
- **Мониторинг** - статистика доставки

### 🔮 Команды для расширенной системы:
```bash
# Будущие команды (в разработке)
python3 sdb.py notifications list          # Список каналов
python3 sdb.py notifications send          # Отправка уведомлений
python3 sdb.py notifications configure     # Настройка каналов
python3 sdb.py notifications test          # Тестирование
python3 sdb.py notifications template      # Управление шаблонами
python3 sdb.py notifications schedule      # Планирование
python3 sdb.py notifications monitor       # Мониторинг
```

## Текущие ограничения

- ✅ Только startup/shutdown уведомления
- ✅ Только Telegram бот (админам)
- ✅ Простое включение/отключение
- ❌ Нет email уведомлений
- ❌ Нет кастомизации сообщений
- ❌ Нет планирования
- ❌ Нет мониторинга доставки
- ❌ Нет шаблонов

## Безопасность

### ✅ Безопасные команды:
- `python3 sdb.py config notifications --enable` - включить уведомления
- `python3 sdb.py config notifications --disable` - отключить уведомления

### ⚠️ Важно:
- Уведомления отправляются только супер-администраторам
- Настройки сохраняются в конфигурационном файле
- Изменения вступают в силу после перезапуска бота

 