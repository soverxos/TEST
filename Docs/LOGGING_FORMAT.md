# 📝 Формат логирования SwiftDevBot

## Обзор

SwiftDevBot поддерживает два формата логирования:
- **Простой формат** (по умолчанию) - только время и сообщение
- **Подробный формат** (с флагом `-v`) - полная информация с модулем, функцией и строкой

## Использование

### Простой формат (по умолчанию)

При обычном запуске бота логи выводятся в простом формате:

```bash
python3 sdb.py run
```

**Пример вывода:**
```
10:23:45 Настройки SDB успешно загружены и провалидированы!
10:23:46 BotServicesProvider и все его базовые сервисы успешно инициализированы.
10:23:47 Экземпляр Telegram Bot успешно создан: @my_bot (ID: 123456789)
```

### Подробный формат (с флагом `-v`)

Для получения подробной информации используйте флаг `-v` или `--verbose`:

```bash
python3 sdb.py run -v
# или
python3 sdb.py run --verbose
```

**Пример вывода:**
```
2025-11-04 10:23:45.123 | SUCCESS  | Systems.core.app_settings:load_app_settings:391 - Настройки SDB успешно загружены и провалидированы!
2025-11-04 10:23:46.456 | INFO     | Systems.core.services_provider:setup_services:156 - BotServicesProvider и все его базовые сервисы успешно инициализированы.
2025-11-04 10:23:47.789 | INFO     | Systems.core.bot_entrypoint:run_sdb_bot:170 - Экземпляр Telegram Bot успешно создан: @my_bot (ID: 123456789)
```

## Комбинация с другими флагами

Флаг `-v` можно комбинировать с другими флагами:

```bash
# Verbose + Debug режим
python3 sdb.py run -v -d

# Verbose + фоновый режим
python3 sdb.py run -v -b
```

## Переменная окружения

Также можно установить переменную окружения `SDB_VERBOSE`:

```bash
export SDB_VERBOSE=true
python3 sdb.py run
```

## Форматы

### Простой формат
```
{time:HH:mm:ss} {message}
```

### Подробный формат
```
{time:YYYY-MM-DD HH:mm:ss.SSS} | {level: <8} | {name}:{function}:{line} - {message}
```

Где:
- `time` - время события
- `level` - уровень логирования (INFO, DEBUG, ERROR, etc.)
- `name` - имя модуля
- `function` - имя функции
- `line` - номер строки
- `message` - текст сообщения

## Примечания

- Файловые логи всегда сохраняются в подробном формате для отладки
- Консольные логи используют выбранный формат
- Флаг `-v` влияет только на консольный вывод, не на файловые логи

